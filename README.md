# Warehouse API

RESTful API сервис для управления складскими запасами рулонов. Проект выполнен на FastAPI с четким разделением на слои (API, Сервисы, Модели).

## 🚀 Стек технологий
* **Python 3.11**
* **FastAPI** — веб-фреймворк.
* **SQLAlchemy** — ORM для работы с БД (SQLite).
* **Pydantic V2** — валидация данных и настройки окружения.
* **Docker** — контейнеризация приложения.
* **Pytest** — автоматизированное тестирование.
* **Mypy** — строгая проверка типизации.

## 🛠 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/henerborn/WarehouseAPI-FastAPI.git
```

### 2. Настройка окружения

Создайте файл .env в корне проекта и заполните его по примеру:
```py
DATABASE_URL=sqlite:///./sql_app.db
PROJECT_TITLE="Warehouse Rolls API"
DEBUG=True
```
### 3.1. Запуск через uvicorn
Из корня проекта:
```bash
uvicorn app.main:app --reload
```

### 3.2. Docker
Сборка образа:
```bash
docker build -t warehouse-api .
```
Запуск контейнера:
```bash
docker run -p 8000:8000 --env-file .env warehouse-api
```

# 📖 Документация API

После запуска сервера интерактивная документация доступна по адресам:

    Swagger UI: http://localhost:8000/docs
    Redoc: http://localhost:8000/redoc

# 🧪 Тестирование и проверка качества
### Запуск тестов
```bash
python -m pytest
```

### Статический анализ (mypy)
Проверка типизации:
```bash
python -m mypy app
```

# 📁 Структура проекта

    app/api — роутеры и эндпоинты.
    app/core — конфигурация приложения.
    app/models — модели SQLAlchemy (БД).
    app/schemas — схемы Pydantic (валидация).
    app/services — бизнес-логика и статистика.
    tests/ — автоматизированные тесты.
